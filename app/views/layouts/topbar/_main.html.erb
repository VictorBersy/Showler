<nav class="top-bar" data-topbar="">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to "Showler", root_url %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href=""></a></li>
  </ul>


  <section class="top-bar-section">
    <ul class="right">
    <% if signed_in? %>
      <%= render "layouts/topbar/right_logged" %>
    <% else %>
      <%= render "layouts/topbar/right_not_logged" %>
    <% end %>
    </ul>
  </section>
</nav>
